import React from 'react';
import SearchField from '../../SearchField/SearchField';

const Search = (props) => {

    return (
        <div>
            <SearchField />
        </div>
    );
};

export default Search;
